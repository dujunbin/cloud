server:
  port: 10090
spring:
  application:
    name: item
  cloud:
    config:
     discovery:
       enabled: true
       service-id: config
     profile: dev
    zookeeper:
      discovery:
        register: true
      connect-string: localhost:2181


